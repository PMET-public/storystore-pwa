name: node-luma-pwa
type: nodejs:10

dependencies:
    nodejs:
        next: ^9.2.0

web:
    commands:
        start: 'next start -p $PORT'

hooks:
    build: |
        npm install
        npm run build

build:
    flavor: none

mounts:
    run:
        source: local
        source_path: run

variables:
    env:
        MAGENTO_URL: https://admin:a6terwtbk67os@develop-sr3snxi-a6terwtbk67os.demo.magentosite.cloud/
        HOME_PAGE_ID: 20
        CATEGORIES_PARENT_ID: 2
        FOOTER_BLOCK_ID: 'footer'

disk: 4096
