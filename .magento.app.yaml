name: node-luma-pwa
type: nodejs:10

dependencies:
    nodejs:
        next: ^9.2.0

web:
    commands:
        start: 'next start -p $PORT'

hooks:
    build: next build
build:
    flavor: none

mounts:
    run:
        source: local
        source_path: run

disk: 4096
