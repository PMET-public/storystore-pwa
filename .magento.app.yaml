name: node-luma-pwa
type: nodejs:10

dependencies:
    nodejs:
        next: ^9.2.0

web:
    commands:
        start: "next start -p $PORT"

hooks:
    build: |
        npm install --quiet
        npm run build

build:
    flavor: none

mounts:
    run:
        source: local
        source_path: run

disk: 4096

variables:
    env:
        MAGENTO_URL: https://admin:a6terwtbk67os@forrester-vhjbjgi-a6terwtbk67os.demo.magentosite.cloud/
        NODE_TLS_REJECT_UNAUTHORIZED: 0
