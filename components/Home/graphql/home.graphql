query HomeQuery($id: Int!, $categoryParentId: Int!) {
    storeConfig {
        id
        baseMediaUrl: secure_base_media_url
    }

    categories: category(id: $categoryParentId) {
        id
        children {
            id
            text: name
            href: url_path
            image
        }
    }

    page: cmsPage(id: $id) {
        id: url_key
        title
        content
        metaTitle: meta_title
        metaDescription: meta_description
        metaKeywords: meta_keywords
    }
}
