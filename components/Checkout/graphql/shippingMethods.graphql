#import "./_ShippingMethods.graphql"

query __ShippingMethodsQuery($cartId: String = "", $hasCart: Boolean = false) {
    cartId @client @export(as: "cartId")
    hasCart @client @export(as: "hasCart")

    cart(cart_id: $cartId) @include(if: $hasCart) {
        ...ShippingMethods
    }
}
