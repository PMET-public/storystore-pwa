#import "./_fragments.graphql"

mutation SetGuesEmailOnCart($cartId: String!, $emailAddress: String!) {
    cartId @client @export(as: "cartId")
    setGuestEmailOnCart(input: { cart_id: $cartId, email: $emailAddress }) {
        cart {
            ...CheckoutDetails
        }
    }
}
