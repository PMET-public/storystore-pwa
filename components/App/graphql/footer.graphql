query AppShellQuery($footerBlockId: String = "", $hasFooter: Boolean = false) {
    footer: cmsBlocks(identifiers: [$footerBlockId]) @include(if: $hasFooter) {
        items {
            id: identifier
            html: content
        }
    }
}
