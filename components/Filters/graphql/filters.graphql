query FiltersQuery {
    filterTypes: __type(name: "ProductAttributeFilterInput") {
        fields: inputFields {
            name
            type {
                name
            }
        }
    }
}
